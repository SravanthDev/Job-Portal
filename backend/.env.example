# Backend Environment Variables Template
# Copy this file to .env and fill in your values

# Server Configuration
NODE_ENV=development
PORT=5001

# Database Configuration (PostgreSQL)
# Get these from your database provider (Render, Neon, Supabase, etc.)
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"
DIRECT_URL="postgresql://user:password@host:5432/database?schema=public"

# JWT Secret (generate a random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# File Upload Configuration
UPLOAD_DIR=./uploads

# Frontend URL (for CORS)
# In development: http://localhost:5173
# In production: https://your-app.vercel.app
FRONTEND_URL=http://localhost:5173

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================
#
# For Render deployment, set these environment variables in Render dashboard:
# 1. NODE_ENV=production
# 2. PORT=5001 (or leave empty, Render sets this automatically)
# 3. DATABASE_URL=<your-database-url>
# 4. DIRECT_URL=<your-direct-database-url>
# 5. JWT_SECRET=<generate-a-strong-random-secret>
# 6. UPLOAD_DIR=./uploads
# 7. FRONTEND_URL=https://your-app.vercel.app
#
# To generate a secure JWT_SECRET, run:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
